---

# doit on forcer le restore de la database même si le dump est deja present ?
forceRestoreDB: false

mysql_port: 3306

# Hostname is important as we need to delete an anonymous MySQL
# user that is created with this name, make sure you match this
# up.
server_hostname: 'steamysql'
user: 'steamulo'

# my.cnf
mysql_max_allowed_packet: '128M'
mysql_character_set_server: 'utf8'
mysql_collation_server: 'utf8_general_ci'

# mysql
mysql_root_password: ''

#mysql_bind_address:




